# Environment Variables
# Copy this file to .env.local and fill in the values

# Site URL (used for metadata and canonical URLs)
NEXT_PUBLIC_SITE_URL=https://networkingforawesomepeople.com

# Decap CMS (configured via Netlify Identity — no env vars needed for git-gateway)
